}

for key, value in pairs(sources) do
    if key:find('mobile_banner_ads') then
      sync {
        initSync, 
        source=key, 
        syncid=value, 
        delete=false
      }
    else
      sync {
        initSync, 
        source=key, 
        syncid=value,
        exclude={"/00**", "/01**"}, 
        delete=false
      }
    end
end

